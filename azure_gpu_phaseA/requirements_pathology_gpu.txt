# Pathology Phase A GPU requirements (Ubuntu 22.04 + CUDA 12.1 + T4)
#
# Install with:
#   pip install --upgrade pip
#   pip install torch==2.4.0 torchvision==0.19.0 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements_pathology_gpu.txt
#
# torch+torchvision pinned to known-good CUDA 12.1 wheels for T4 driver compat.
# Pillow-SIMD optional for faster JPEG/PNG decode but skip for portability.

numpy>=1.26,<2.2
pandas>=2.1,<2.3
scipy>=1.11,<1.15
scikit-learn>=1.3,<1.6
statsmodels>=0.14,<0.16
matplotlib>=3.8,<3.10
Pillow>=10.0,<11.2
anndata>=0.10,<0.11
h5py>=3.10,<3.13
tqdm>=4.66

"""MCP-server-shaped tool stubs.

Each module exports plain Python functions today; the same surface translates
1:1 to MCP tool definitions in v0.3 (live wiring). Naming follows the
brainstorm spec (rfp_reader_mcp, biodata_reuse_mcp, ...).
"""

# CROSS-Neo 2.0 Model Families

The first-pass sprint uses scikit-learn implementations in `04_train_models.py` so the full locked evaluation is runnable on CPU. Heavy ESM2/ProtT5/Boltz/Chai/AF3 replacements should write into the same feature files declared by `features/feature_manifest.json`.

Public predictor scores are excluded from model features.
